# HTTP API base URL
VITE_API_URL=http://localhost:8000

# WebSocket URL — explicit ws:// protocol.
# Do NOT rely on the Vite proxy for the WebSocket; it connects directly.
VITE_WS_URL=ws://localhost:8000/ws
